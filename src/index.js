import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './Nivel-1-Basico/ejercicio-5/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);